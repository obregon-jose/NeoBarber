import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RegistropeluqueroPage } from './registropeluquero.page';

describe('RegistropeluqueroPage', () => {
  let component: RegistropeluqueroPage;
  let fixture: ComponentFixture<RegistropeluqueroPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RegistropeluqueroPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
